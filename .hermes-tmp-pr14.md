Implements the merged spec `add-power-origin-transparency` — where the power comes from, which current type, who draws it.

**Power origin per settlement**
- Origin line under every lit settlement: „Eigenversorgung" when its own assigned plant runs, „Strom aus: {plant}" naming the feeding plant for grid-fed settlements, nothing when dark
- The feeding plant is the nearest running anchor — the same plant the distribution line is drawn from, so caption and line never contradict each other

**Current-type badges**
- ⎓ / ~ badge on every CityView plant icon (also while under construction) and on every PlantPanel plant entry
- Derived from the generator catalog via the new `plantCurrentType()`; the catalog is DC-only until `add-three-phase-power` adds the alternator — data unchanged, the field defaults to `dc`

**Customer mix panel**
- New `CustomerMixPanel.svelte` in the right column below the tariff: per settlement the household-weighted average plus wealthy/average/poor in percent, from the real `growth.shares`
- Live-verified in the browser: percentages move with each quarter tick

**Notable**
- Spec fix included: the "household-weighted average" scenario said 54 % where the numbers give 58 % (699 of 1,200 households) — corrected with the arithmetic shown in the delta
- Branch merges `fix/region-grid-lighting`: the origin scenarios presuppose the region-wide grid (a fed village naming the plant needs a lit village), and both changes touch CityView — one merged truth instead of a conflict later
- Pure display layer: no sim core, no data, SAVE_VERSION stays 3

**Verification:** 168/168 tests (10 new: origin grid-fed/self-supply/dark, badge operational/under construction, mix percentages/weighted average/rounding/update), svelte-check 0/0, clean build, browser spot-check green.
